<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verified Successfully</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; font-family:
      -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial,
      sans-serif; } body { background: linear-gradient(135deg, #e8f5e9 0%,
      #c8e6c9 100%); min-height: 100vh; display: flex; justify-content: center;
      align-items: center; padding: 20px; } .container { background: white;
      border-radius: 20px; box-shadow: 0 15px 40px rgba(0, 0, 0, 0.12); width:
      100%; max-width: 450px; overflow: hidden; text-align: center; transition:
      all 0.3s ease; animation: fadeIn 0.5s ease-out; } .header { background:
      linear-gradient(135deg, #4caf50 0%, #2e7d32 100%); color: white; padding:
      40px 20px; position: relative; overflow: hidden; } .header::after {
      content: ''; position: absolute; top: -50%; left: -50%; width: 200%;
      height: 200%; background: rgba(255, 255, 255, 0.1); transform:
      rotate(30deg); } .icon { font-size: 70px; margin-bottom: 15px; position:
      relative; z-index: 1; animation: bounce 1s ease; } .content { padding:
      40px 30px; } h1 { font-size: 28px; margin-bottom: 15px; color: #333;
      font-weight: 700; } p { color: #666; line-height: 1.6; margin-bottom:
      20px; font-size: 16px; } .footer { padding: 22px; background: #f9f9f9;
      color: #888; font-size: 14px; } .success-message { background: #f1f8e9;
      border-radius: 10px; padding: 15px; margin: 20px 0; border-left: 4px solid
      #4caf50; } .checkmark { font-size: 64px; color: #4caf50; margin-bottom:
      24px; animation: scaleIn 0.8s ease; } @keyframes fadeIn { from { opacity:
      0; transform: translateY(20px); } to { opacity: 1; transform:
      translateY(0); } } @keyframes bounce { 0%, 20%, 50%, 80%, 100% {
      transform: translateY(0); } 40% { transform: translateY(-12px); } 60% {
      transform: translateY(-6px); } } @keyframes scaleIn { 0% { transform:
      scale(0); opacity: 0; } 70% { transform: scale(1.2); } 100% { transform:
      scale(1); opacity: 1; } } /* Mobile Responsive Styles */ @media
      (max-width: 480px) { body { padding: 15px; display: flex; background:
      linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%); } .container {
      border-radius: 12px; box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
      max-width: 100%; align-self: center; margin: auto 0; } .header { padding:
      30px 15px; } .icon { font-size: 60px; } h1 { font-size: 24px; } .content {
      padding: 30px 20px; } p { font-size: 15px; } .footer { padding: 18px; }
      .checkmark { font-size: 56px; margin-bottom: 20px; } } /* Small Mobile
      Devices */ @media (max-width: 350px) { .header { padding: 25px 15px; }
      .icon { font-size: 50px; } h1 { font-size: 22px; } .content { padding:
      25px 15px; } .success-message { padding: 12px; } .checkmark { font-size:
      48px; } }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <div class="icon">
          <i class="fas fa-check-circle"></i>
        </div>
        <h1>Email Verified!</h1>
      </div>

      <div class="content">
        <div class="checkmark">✓</div>
        <p>
          Your email has been successfully verified. You can now return to the
          app and login
        </p>

        <div class="success-message">
          <p>
            <i class="fas fa-info-circle" style="color: #4caf50;"></i>
            You can safely close this window
          </p>
        </div>
      </div>

      <div class="footer">
        <p>Thank you for verifying your email address</p>
      </div>
    </div>
  </body>
</html>