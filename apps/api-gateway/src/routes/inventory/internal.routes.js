import express from "express";
// import { getKitchensAccessibleToProperty } from "../../controllers/inventory/internal.controller.js";

const internalRoutes = express.Router();

// internalRoutes.get("/kitchens/accessible", getKitchensAccessibleToProperty);

export default internalRoutes;
