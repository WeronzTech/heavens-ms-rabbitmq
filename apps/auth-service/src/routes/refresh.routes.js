import express from "express";
// import { refreshAccessToken } from '../controllers/refresh.controller.js';

const router = express.Router();

// router.post('/refresh', refreshAccessToken); // Changed to POST

export default router;
