import express from "express";
// import {getRoleByName} from "../controllers/internal.controller.js";

const router = express.Router();

// router.get("/role/:roleName", getRoleByName);

export default router;
